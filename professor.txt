[* this is comment for this sample code for assignment 3 *]
#
integer i, max, sum; [* declarations *]
bool y, n;

sum = 0;
i = 1;
get (max);
while (i < max) {
sum = sum + i;
i = i + 1;
}
put (sum + max);
#
